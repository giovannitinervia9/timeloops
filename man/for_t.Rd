% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeloops.R
\name{for_t}
\alias{for_t}
\title{Set up a \code{for} loop with progress reporting}
\usage{
for_t(..., each = 1L, cores = 1L)
}
\arguments{
\item{...}{A single named argument of the form \code{var = sequence}, giving the
loop variable and the values to iterate over.}

\item{each}{Print an update every \code{each} iterations. Defaults to \code{1}. Used
only by \verb{\%do\%}.}

\item{cores}{Number of parallel workers to use with \verb{\%dopar\%}. Defaults to
\code{1}. The cluster is created and torn down automatically and works on all
platforms, including Windows.}
}
\value{
A loop specification (class \code{tl_loop}) to be piped into \code{\link[=grapes-do]{\%do\%}} or
\code{\link[=grapes-do]{\%dopar\%}}.
}
\description{
Describes a \code{for} loop to be run with \code{\link[=grapes-do]{\%do\%}} (sequential) or
\code{\link[=grapes-do]{\%dopar\%}} (parallel). Use the natural \code{var = sequence} form:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{for_t(i = 1:n, each = 10) \%do\% \{ ... \}
for_t(i = 1:n, cores = 4) \%dopar\% \{ ... \}
}\if{html}{\out{</div>}}

With \verb{\%do\%} it behaves exactly like a base \code{for} loop (same scope, same
\code{next}/\code{break}) while printing progress and an estimated time to completion
(ETA). With \verb{\%dopar\%} the iterations run in parallel and the loop returns a
list of results (see \code{\link[=grapes-do]{\%do\%}} for the difference in behaviour).
}
\examples{
# sequential: behaves like a normal for loop, with live progress + ETA
for_t(i = 1:5) \%do\% {
  Sys.sleep(0.01)
}

# build up a result in the calling scope, exactly like base `for`
total <- 0
for_t(i = 1:10, each = 5) \%do\% {
  total <- total + i
}
total

# parallel: returns a list of results (no side effects), with a progress bar
\donttest{
squares <- for_t(i = 1:8, cores = 2) \%dopar\% {
  i^2
}
}
}
\seealso{
\code{\link[=grapes-do]{\%do\%}}, \code{\link[=grapes-do]{\%dopar\%}}, \code{\link[=while_t]{while_t()}}, \code{\link[=repeat_t]{repeat_t()}}
}
