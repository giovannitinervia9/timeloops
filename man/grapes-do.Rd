% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeloops.R
\name{grapes-do}
\alias{grapes-do}
\alias{\%do\%}
\alias{\%dopar\%}
\title{Run a timed loop}
\usage{
loop \%do\% expr

loop \%dopar\% expr
}
\arguments{
\item{loop}{A loop specification from \code{\link[=for_t]{for_t()}}, \code{\link[=while_t]{while_t()}} or \code{\link[=repeat_t]{repeat_t()}}.}

\item{expr}{The loop body.}
}
\value{
\verb{\%do\%} returns \code{NULL} invisibly (called for its side effects).
\verb{\%dopar\%} returns a list with one element per iteration.
}
\description{
\verb{\%do\%} runs the loop set up by \code{\link[=for_t]{for_t()}}, \code{\link[=while_t]{while_t()}} or \code{\link[=repeat_t]{repeat_t()}}
\strong{sequentially}, exactly like the corresponding base loop: the body is
evaluated in the calling scope (so assignments and \verb{<-} updates are visible
afterwards), \code{next} and \code{break} work as usual, and progress is printed while
it runs. It returns \code{NULL} invisibly.

\verb{\%dopar\%} runs a \code{\link[=for_t]{for_t()}} loop \strong{in parallel} across \code{cores} workers. Each
iteration runs in its own process, so the body must be self-contained and
return a value: \verb{\%dopar\%} collects these and returns them as a list, and
changes to the calling environment are \emph{not} visible. A progress bar with
elapsed time is shown. Parallel execution is only available for \code{for} loops.
}
\section{Customising the parallel progress bar}{

The parallel progress bar is produced with the \pkg{progressr} package, so its
appearance is fully under your control: pick a handler (and its options) once,
before running the loop, and every \verb{\%dopar\%} call will use it. For example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# choose a different style
progressr::handlers("progress")   # or "cli", "txtprogressbar", "rstudio", ...

# customise the format (tokens like :percent, :elapsed, :eta, :bar)
progressr::handlers(progressr::handler_progress(
  format = "Working :percent | elapsed :elapsed | eta :eta"
))

for_t(i = 1:1000, cores = 4) \%dopar\% slow_function(i)
}\if{html}{\out{</div>}}

See \code{\link[progressr:handlers]{progressr::handlers()}} for the full list of handlers and options. If you
do not set anything, a plain text progress bar is used.
}

\examples{
for_t(i = 1:3) \%do\% {
  Sys.sleep(0.01)
}

\donttest{
for_t(i = 1:6, cores = 2) \%dopar\% {
  i * 10
}

# customise the progress bar before running
progressr::handlers("txtprogressbar")
for_t(i = 1:6, cores = 2) \%dopar\% {
  Sys.sleep(0.05)
  i * 10
}
}
}
\seealso{
\code{\link[=for_t]{for_t()}}, \code{\link[=while_t]{while_t()}}, \code{\link[=repeat_t]{repeat_t()}}, \code{\link[progressr:handlers]{progressr::handlers()}}
}
